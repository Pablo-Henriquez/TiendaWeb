@model IEnumerable<TiendaWeb.Models.Compania>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Agregar marca", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nombre)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.IdCompania }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.IdCompania }) |
            @Html.ActionLink("Eliminar", "Delete", new { id=item.IdCompania })
        </td>
    </tr>
}

</table>
